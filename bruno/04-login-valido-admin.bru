meta {
  name: 04 Login valido ADMIN
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "admin@cantina.com",
    "senha": "Admin@123"
  }
}

script:post-response {
  if (res.body && res.body.token) {
    bru.setEnvVar("tokenAdmin", res.body.token);
  }
}
